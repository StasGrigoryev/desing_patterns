package org.example.patterns.creational.absractFactory.checkboxes;

/*
    Checkboxes are the second product family.
    It has the same variations as buttons do.
 */
public interface Checkbox {
    void paint();
}